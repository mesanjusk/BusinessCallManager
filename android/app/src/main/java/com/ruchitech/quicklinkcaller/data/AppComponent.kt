package com.ruchitech.quicklinkcaller.data
/*

import android.app.Application
import com.ruchitech.quicklinkcaller.helper.PreferenceModule
import com.ruchitech.quicklinkcaller.persistence.recievers.TriggerReceiver
import dagger.BindsInstance
import dagger.Component
import javax.inject.Singleton

@Singleton
@Component(modules = [PreferenceModule::class])
interface AppComponent {

    fun inject(receiver: TriggerReceiver)

    @Component.Builder
    interface Builder {

        @BindsInstance
        fun application(application: Application): Builder

        fun build(): AppComponent
    }
}
*/
